# Dependencies
node_modules

# Build output
dist
dist-ssr
.output
.nitro
.tanstack
.vinxi
.wrangler

# Generated by TanStack Router
src/routeTree.gen.ts

# Env — commit only the example
.env
.env.*
!.env.example
*.local

# Migration pipeline resume caches — the committed artifacts are the record
data/migration/*/cache/

# Test / coverage artifacts
coverage
/test-results
/playwright-report
/playwright/.cache

# Supabase local stack (if `supabase init` is run)
supabase/.branches
supabase/.temp

# TypeScript / tooling caches
*.tsbuildinfo
__unconfig*
todos.json

# Logs
*.log

# OS / editor
.DS_Store
.idea

# Personal setup, Claude plugin/skill artifacts, and private planning docs
# (kept local, not committed). Project knowledge that DOES belong in VCS
# stays tracked: README.md, CONTEXT.md, docs/adr/.
/CLAUDE.md
/AGENTS.md
/.no-mistakes.json
/docs/agents/
/GRB-Records-PRD.md
/WT-Records-Build-Handoff.md

# Personal Claude Code overrides — never shared
.claude/settings.local.json

# The impeccable skill is a vendored tool, not project code — reinstall with
# `npx impeccable install`. Its project artifacts (PRODUCT.md, DESIGN.md,
# .impeccable/) ARE committed.
.claude/skills/impeccable/

# Local design prototype (referenced by the PRD, which is also local-only)
wt-glass-concept.html

# design-sync machine state (staged scripts, build output, caches)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
